import styled from 'styled-components';

import { Colors } from 'src/utils/colors';

export const DatePopup = styled.div`
    background: ${Colors.extra.greyLightest};
    border-radius: 12px;
    width: fit-content;
    padding: 8px 16px;
    margin: 1em auto;
    font-size: 12px;
    font-weight: 600;
    color: ${Colors.extra.black};
`;
